.reply
  ul.replies
    - each reply in vote.replies
      != partial('replies/reply.html.jade', { locals: { reply: reply } })
  - if (ctx.canReplyTo(vote))
    a.reply( href: '#' ) Reply
    form( method: 'POST', action: '/teams/' + vote.team.toParam() + '/votes/' + vote.toParam() + '/replies' )
      ul.errors
      textarea( name: 'body', placeholder: 'Leave a reply.' )
      input.button( type: 'submit', value: 'Reply' )
