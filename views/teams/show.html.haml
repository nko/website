.note
  This is it for now.  We&rsquo;ll likely ask for more
  details soon.

%h1= this.team.name
- if (this.members)
  %h2 Members
  %ul
    - each member in this.members
      %li
        = member.name || member.email
        %a{ href: '#'} Reinvite
        %a{ href: '/people/' + member.id() + '/edit' } Edit
        %a{ href: '#'} Remove
%form{ method: "POST", action: "/teams/" + this.team.id() }
  %input{ type: "hidden", name: "_method", value: "delete" }
  %input{ type: "submit", name: "delete", value: "Delete this team?"}