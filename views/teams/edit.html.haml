%h1 Edit Team

!= this.partial("errors.html.haml")

%form{ method: 'POST', action: '/teams/' + this.team.id() + '?_method=PUT' }
  != this.partial('teams/form.html.haml')
  %p
    %input.button{ type: 'submit', value: 'Save' }